<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Dashboard - Encontre Uma Banda</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="dashboard-body">

  <header>
    <h1>Bem-vindo, <%= user.username %>!</h1>
    <nav>
      <a href="/auth/logout">Sair</a>
    </nav>
  </header>

  <main>
    <form action="/search/results" method="GET" class="search-form">
      <input type="text" name="q" placeholder="Digite o nome da banda ou artista..." required />
      <button type="submit">Buscar</button>
    </form>

    <% if (artistInfo) { %>
      <section class="artist-details">
        <img src="<%= artistInfo.image || '/images/default-artist.png' %>" alt="Foto do artista <%= artistInfo.name %>" />
        <div class="artist-text">
          <h2><%= artistInfo.name %></h2>
          <p><%= artistInfo.bio || 'Biografia não disponível.' %></p>
        </div>
      </section>
    <% } %>
  </main>

</body>
</html>
