<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Resultados - <%= artistInfo ? artistInfo.name : 'Artista não encontrado' %></title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="dashboard-body">
  <main class="container">

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } else if (artistInfo) { %>
      <h1><%= artistInfo.name %></h1>
      
      <div class="artist-container">
        <% if (artistImage) { %>
          <img src="<%= artistImage %>" alt="Foto do artista <%= artistInfo.name %>" />
        <% } else { %>
          <p>Imagem não disponível.</p>
        <% } %>

        <div class="artist-bio">
          <p><%- artistInfo.bio ? artistInfo.bio.summary : 'Biografia não disponível.' %></p>
          <% if (artistInfo.link) { %>
            <p><a href="<%= artistInfo.link %>" target="_blank" rel="noopener noreferrer">Perfil Deezer</a></p>
          <% } %>
        </div>
      </div>

      <h2>Videoclipes</h2>
      <div class="videos-container">
        <% if (videos && videos.length > 0) { %>
          <% videos.forEach(video => { %>
            <iframe width="320" height="180" src="<%= video %>" frameborder="0" allowfullscreen></iframe>
          <% }) %>
        <% } else { %>
          <p>Vídeos não encontrados.</p>
        <% } %>
      </div>

    <% } else { %>
      <p>Nenhum artista encontrado para sua busca.</p>
    <% } %>

  </main>
</body>
</html>
